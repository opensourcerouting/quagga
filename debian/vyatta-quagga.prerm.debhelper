# Automatically added by dh_installinit
if [ -x "/etc/init.d/vyatta-quagga" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d vyatta-quagga stop || exit $?
	else
		/etc/init.d/vyatta-quagga stop || exit $?
	fi
fi
# End automatically added section

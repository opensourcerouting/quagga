# Automatically added by dh_installinit
if [ -x "/etc/init.d/vyatta-quagga" ]; then
	update-rc.d vyatta-quagga defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d vyatta-quagga start || exit $?
	else
		/etc/init.d/vyatta-quagga start || exit $?
	fi
fi
# End automatically added section
